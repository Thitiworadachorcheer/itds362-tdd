**1. มีเทสต์ทั้งหมดกี่ตัวและโค้ดจริงกี่บรรทัด อัตราส่วนเป็นเท่าใด**
- เทส: 3 ตัว
- โค้ด: 73 บรรทัด
- อัตราส่วน: ประมาณ 1 : 24.3 (3 tests / 73 lines ≈ 4.1%)


**2. ช่วงที่แถบเป็นสีแดงนานที่สุดคือเมื่อใด? อะไรทำให้มันนาน?**
- ช่วงที่ยาวที่สุดคือตอนที่เปลี่ยน `Quantity.plus` ให้คืนอ็อบเจ็กต์ `Sum` (ขั้นตอน TDD หลังจาก fake-it) แต่ยังไม่ได้เขียน `Sum.reduce` และยังไม่ปรับ `Converter` ให้ delegate การลดให้กับ `Sum` เทสต์ก็ยังไม่ได้ import `Converter` ในทันที ผลคือเทสล้มเหลวหลายกรณีจนกว่าจะเพิ่ม `Sum.reduce` และแก้ไขการนำเข้าในเทสให้ครบถ้วน
- เหตุผลที่ใช้เวลานาน: ต้องคิดการแยกความรับผิดชอบ และต้องแก้ทั้งโค้ดและเทสให้สอดคล้องกันก่อนเทสต์จะเขียว


**3. จุดที่ใช้เทคนิคต่างๆ**
- Fake It: ใช้ตอนเริ่มต้น โดยให้ `plus` และ `Converter.reduce` ทำงานแบบง่าย ๆ เพื่อทำให้เทสเขียวเร็ว 
เหมาะสม เพราะช่วยยืนยันการคำนวณเทสได้เร็วและลดความซับซ้อนชั่วคราว
- Triangulate: ใช้เมื่อย้ายจาก fake implementation → คืน `Sum` → เพิ่ม `Sum.reduce` เพื่อให้ behaviour ถูกต้องสำหรับหลายกรณี เหมาะสม เพราะช่วยแยกหน้าที่ทีละขั้นและป้องกันการทำงานผิดพลาดของโค้ดที่ซับซ้อน
- Obvious Implementation: ใช้เมื่อเขียน `Sum.reduce` (รวมจำนวนจากสองฝั่งโดยตรง) เหมาะสม เพราะการรวมตัวเลขเป็นการทำงานที่ชัดเจนและไม่ต้องการ optimization หรือโครงสร้างที่ซับซ้อนในขั้นแรก


**4. รายการทดสอบ (Test List)**
- รายการที่ค้างอยู่:
	- เทสต์การรวมหน่วยต่างกัน (mixed-unit addition) ที่แปลงหน่วยด้วยอัตราแปลงจริง
	- เทสต์ nested sums (เช่น `(200g + 1oz) + 300g`)
- รายการที่จะทำต่อ:
	- เพิ่มเมดทอด `Quantity.reduce(unit)` เพื่อให้ `Quantity` สามารถถูกลด/แปลงกลับมาเป็นหน่วยเป้าหมายได้ (ปัจจุบันใช้ `hasattr` เป็นเบื้องต้น)
	- เพิ่มตารางอัตราแปลงหน่วยและให้ `Converter` หรือ `Quantity.reduce` ใช้ตารางนั้น
	- เพิ่มเทสต์สำหรับการแปลงหน่วยและกรณี edge (เช่นค่าลบหรือ zero)
- รายการที่ควรลบทิ้ง:
	- ไม่มีเทสซ้ำเหลืออยู่แล้ว (ก่อนหน้านี้มีเทสต์คูณซ้ำหลายอัน รวมเป็นเคสเดียวที่ชัดเจนแล้ว)


**5. เมท็อด Quantity.reduce**
- ถ้าออกแบบก่อนจะใส่ Quantity.reduce แต่เราใช้ TDD จึงเพิ่มเป็น refactor ถัดไป